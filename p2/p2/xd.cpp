
    // Enlosar(M,n-1,row*!( lower  &&  right ),col*!( lower  &&  right ),first_row,first_col);
    // Enlosar(M,n-1,row* (!lower  &&  right ),col* (!lower  &&  right ),first_row+middle,first_col);
    // Enlosar(M,n-1,row* ( lower  && !right ),col* ( lower  && !right ),first_row,first_col+middle);
    // Enlosar(M,n-1,row* ( lower  &&  right ),col* ( lower  &&  right ),first_row+middle,first_col+middle);